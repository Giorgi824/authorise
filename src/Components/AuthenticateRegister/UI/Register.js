import React from "react";

function Register() {
  const styles = {
    display: "flex",
    flexGrow: "1",
    color: "#fff",
    justifyContent: "center",
    alignItems: "center",
    fontSize: "4rem",
  };
  return <div style={styles}>Register</div>;
}

export default Register;
