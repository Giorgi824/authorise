import React from "react";

function ForgetPassword() {
  const styles = {
    display: "flex",
    flexGrow: "1",
    color: "#fff",
    justifyContent: "center",
    alignItems: "center",
    fontSize: "4rem",
  };
  return <div style={styles}>ForgetPassword</div>;
}

export default ForgetPassword;
